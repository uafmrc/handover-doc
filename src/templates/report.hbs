<!DOCTYPE html>
<html lang="{{locale}}" class="scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{projectName}} - {{t "readme.title"}}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/11.9.0/styles/github-dark.min.css">
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                        mono: ['JetBrains Mono', 'monospace'],
                    },
                    colors: {
                        gray: {
                            850: '#1f2937',
                            900: '#111827',
                            950: '#030712',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        /* Custom scrollbar */
        ::-webkit-scrollbar {
            width: 8px;
            height: 8px;
        }
        ::-webkit-scrollbar-track {
            background: transparent;
        }
        ::-webkit-scrollbar-thumb {
            background: #cbd5e1;
            border-radius: 4px;
        }
        .dark ::-webkit-scrollbar-thumb {
            background: #4b5563;
        }
        ::-webkit-scrollbar-thumb:hover {
            background: #94a3b8;
        }
        .dark ::-webkit-scrollbar-thumb:hover {
            background: #6b7280;
        }

        .glass-panel {
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
        }
        .dark .glass-panel {
            background: rgba(17, 24, 39, 0.7);
        }
    </style>
</head>
<body class="bg-gray-50 text-gray-800 font-sans antialiased transition-colors duration-300 dark:bg-gray-950 dark:text-gray-100">
    
    <!-- Sidebar -->
    <aside class="fixed top-0 left-0 z-40 w-64 h-screen transition-transform -translate-x-full sm:translate-x-0 bg-white dark:bg-gray-900 border-r border-gray-200 dark:border-gray-800 shadow-lg">
        <div class="h-full px-3 py-4 overflow-y-auto flex flex-col">
            <div class="mb-5 px-2">
                <h2 class="text-xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-blue-600 to-indigo-600 truncate">
                    {{projectName}}
                </h2>
                <p class="text-xs text-gray-500 dark:text-gray-400 mt-1">Handover Docs</p>
            </div>
            
            <ul class="space-y-2 font-medium flex-1">
                <li>
                    <a href="#overview" class="flex items-center p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-800 group transition-colors">
                        <span class="mr-3">📊</span>
                        <span class="flex-1 whitespace-nowrap">{{t "html.overview"}}</span>
                    </a>
                </li>
                <li>
                    <a href="#architecture" class="flex items-center p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-800 group transition-colors">
                        <span class="mr-3">🏗️</span>
                        <span class="flex-1 whitespace-nowrap">{{t "html.architecture"}}</span>
                    </a>
                </li>
                <li>
                    <a href="#mindmap" class="flex items-center p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-800 group transition-colors">
                        <span class="mr-3">🗺️</span>
                        <span class="flex-1 whitespace-nowrap">{{t "html.fileMap"}}</span>
                    </a>
                </li>
                <li>
                    <a href="#dependencies" class="flex items-center p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-800 group transition-colors">
                        <span class="mr-3">📦</span>
                        <span class="flex-1 whitespace-nowrap">{{t "html.dependencies"}}</span>
                    </a>
                </li>
                <li>
                    <a href="#files" class="flex items-center p-2 text-gray-900 rounded-lg dark:text-white hover:bg-gray-100 dark:hover:bg-gray-800 group transition-colors">
                        <span class="mr-3">📄</span>
                        <span class="flex-1 whitespace-nowrap">{{t "html.files"}}</span>
                        <span class="inline-flex items-center justify-center w-3 h-3 p-3 ms-3 text-sm font-medium text-blue-800 bg-blue-100 rounded-full dark:bg-blue-900 dark:text-blue-300">{{totalFiles}}</span>
                    </a>
                </li>
            </ul>

            <div class="pt-4 mt-4 border-t border-gray-200 dark:border-gray-700">
                <button id="theme-toggle" class="flex items-center w-full p-2 text-gray-900 transition duration-75 rounded-lg hover:bg-gray-100 dark:hover:bg-gray-700 dark:text-white group">
                    <svg id="theme-toggle-dark-icon" class="hidden w-5 h-5 text-gray-500 dark:text-gray-400 group-hover:text-blue-600 dark:group-hover:text-yellow-400 mr-3" fill="currentColor" viewBox="0 0 20 20"><path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path></svg>
                    <svg id="theme-toggle-light-icon" class="hidden w-5 h-5 text-gray-500 dark:text-gray-400 group-hover:text-orange-500 mr-3" fill="currentColor" viewBox="0 0 20 20"><path d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" fill-rule="evenodd" clip-rule="evenodd"></path></svg>
                    <span class="flex-1 whitespace-nowrap">{{t "html.changeTheme"}}</span>
                </button>
            </div>
            
            <div class="pt-4 text-xs text-gray-400 text-center">
                {{t "html.generatedOn"}} {{generatedDate}}
            </div>
        </div>
    </aside>

    <!-- Main Content -->
    <div class="p-4 sm:ml-64 min-h-screen">
        <div class="p-4 border-2 border-dashed border-gray-200 dark:border-gray-700 rounded-lg max-w-7xl mx-auto">
            
            <!-- Hero Section -->
            <div class="relative bg-gradient-to-r from-blue-600 to-indigo-700 rounded-xl shadow-xl overflow-hidden mb-8 p-8 text-white">
                <div class="relative z-10">
                    <h1 class="text-3xl md:text-5xl font-bold mb-2">{{projectName}}</h1>
                    <p class="text-blue-100 text-lg opacity-90">{{t "html.technicalHandover"}}</p>
                </div>
                <div class="absolute right-0 top-0 h-full w-1/3 opacity-10 bg-[url('data:image/svg+xml;base64,PHN2ZyB3aWR0aD0iMjAiIGhlaWdodD0iMjAiIHhtbG5zPSJodHRwOi8vd3d3LnczLm9yZy8yMDAwL3N2ZyI+PGNpcmNsZSBjeD0iMSIgY3k9IjEiIHI9IjEiIGZpbGw9IiNmZmYiLz48L3N2Zz4=')]"></div>
            </div>

            <!-- Dashboard Stats -->
            <div id="overview" class="grid grid-cols-1 gap-4 mb-8">
                <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700">
                    <div class="flex items-center">
                        <div class="p-3 rounded-full bg-indigo-100 dark:bg-indigo-900 text-indigo-600 dark:text-indigo-300 mr-4">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.384-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"></path></svg>
                        </div>
                        <div>
                            <p class="text-sm text-gray-500 dark:text-gray-400">{{t "html.framework"}}</p>
                            <h3 class="text-xl font-bold text-gray-800 dark:text-gray-100">{{#if framework}}{{framework}}{{else}}Custom{{/if}}</h3>
                        </div>
                    </div>
                </div>
                <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700">
                    <div class="flex items-center">
                        <div class="p-3 rounded-full bg-blue-100 dark:bg-blue-900 text-blue-600 dark:text-blue-300 mr-4">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path></svg>
                        </div>
                        <div>
                            <p class="text-sm text-gray-500 dark:text-gray-400">{{t "html.totalFiles"}}</p>
                            <h3 class="text-2xl font-bold text-gray-800 dark:text-gray-100">{{totalFiles}}</h3>
                        </div>
                    </div>
                </div>
                <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700">
                    <div class="flex items-center">
                        <div class="p-3 rounded-full bg-green-100 dark:bg-green-900 text-green-600 dark:text-green-300 mr-4">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4"></path></svg>
                        </div>
                        <div>
                            <p class="text-sm text-gray-500 dark:text-gray-400">{{t "html.totalLines"}}</p>
                            <h3 class="text-2xl font-bold text-gray-800 dark:text-gray-100">{{totalLines}}</h3>
                        </div>
                    </div>
                </div>
                <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700">
                    <div class="flex items-center">
                        <div class="p-3 rounded-full bg-purple-100 dark:bg-purple-900 text-purple-600 dark:text-purple-300 mr-4">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.384-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"></path></svg>
                        </div>
                        <div>
                            <p class="text-sm text-gray-500 dark:text-gray-400">{{t "html.languages"}}</p>
                            <h3 class="text-2xl font-bold text-gray-800 dark:text-gray-100">{{languagesCount}}</h3>
                        </div>
                    </div>
                </div>
                <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700">
                    <div class="flex items-center">
                        <div class="p-3 rounded-full bg-yellow-100 dark:bg-yellow-900 text-yellow-600 dark:text-yellow-300 mr-4">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path></svg>
                        </div>
                        <div>
                            <p class="text-sm text-gray-500 dark:text-gray-400">{{t "html.functions"}}</p>
                            <h3 class="text-2xl font-bold text-gray-800 dark:text-gray-100">{{functionsCount}}</h3>
                        </div>
                    </div>
                </div>

                <!-- API Routes & Tech Debt -->
                <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 relative">
                    <div class="flex items-center">
                         <div class="p-3 rounded-full {{#if (eq techDebt.level 'Low')}}bg-green-100 text-green-600 dark:bg-green-900 dark:text-green-300{{else if (eq techDebt.level 'Medium')}}bg-yellow-100 text-yellow-600 dark:bg-yellow-900 dark:text-yellow-300{{else}}bg-red-100 text-red-600 dark:bg-red-900 dark:text-red-300{{/if}} mr-4">
                            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                        </div>
                        <div>
                            <div class="flex items-center gap-1">
                                <p class="text-sm text-gray-500 dark:text-gray-400">{{t "html.techDebt"}}</p>
                                <div class="group relative flex">
                                    <svg class="w-4 h-4 text-gray-400 cursor-help hover:text-indigo-500 transition-colors" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
                                    <div class="absolute bottom-full left-0 mb-2 hidden group-hover:block w-72 p-3 bg-gray-900 dark:bg-gray-700 text-white text-xs rounded-lg shadow-xl z-50 pointer-events-none">
                                        <p class="font-bold mb-1 border-b border-gray-700 pb-1">{{t "html.techDebtTitle"}}</p>
                                        <p class="opacity-90 leading-relaxed">{{t "html.techDebtDescription"}}</p>
                                        <ul class="mt-2 space-y-1 text-[10px]">
                                            <li>• <strong>{{t "html.complexity"}}:</strong> {{t "html.techDebtComplexity"}}</li>
                                            <li>• <strong>TODO/FIXME:</strong> {{t "html.techDebtTodo"}}</li>
                                        </ul>
                                    </div>
                                </div>
                            </div>
                            <h3 class="text-2xl font-bold text-gray-800 dark:text-gray-100">{{techDebt.level}}</h3>
                            <span class="text-xs text-gray-400">Score: {{techDebt.score}}</span>
                        </div>
                    </div>
                </div>

            <!-- Project Summary (Full Width) -->
            {{#if projectSummary}}
            <div class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 mb-8">
                <h3 class="text-xl font-bold text-gray-800 dark:text-gray-200 mb-4 border-b border-gray-100 dark:border-gray-700 pb-2">{{t "html.executiveSummary"}}</h3>
                <div class="prose prose-blue dark:prose-invert max-w-none text-gray-600 dark:text-gray-300 leading-relaxed">
                    {{{projectSummary}}}
                </div>
            </div>
            {{/if}}

            <!-- Charts Section (Grid) -->
            <div class="grid grid-cols-1 gap-8 mb-8">
                 <!-- Language Chart -->
                 <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700">
                    <div class="flex justify-between items-center mb-4 border-b border-gray-100 dark:border-gray-700 pb-2">
                         <h3 class="text-lg font-bold text-gray-800 dark:text-gray-200">{{t "html.languages"}}</h3>
                         <span class="text-xs font-mono text-gray-400">{{t "html.languagesDistribution"}}</span>
                    </div>
                    <div class="h-[300px] w-full flex justify-center items-center">
                        <canvas id="languagesChart"></canvas>
                    </div>
                </div>

                <!-- Complexity Chart -->
                <div class="bg-white dark:bg-gray-800 p-6 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700">
                    <div class="flex justify-between items-center mb-4 border-b border-gray-100 dark:border-gray-700 pb-2">
                         <h3 class="text-lg font-bold text-gray-800 dark:text-gray-200">{{t "html.complexity"}}</h3>
                         <span class="text-xs font-mono text-gray-400">{{t "html.complexityFileAnalysis"}}</span>
                    </div>
                    <div class="h-[300px] w-full flex justify-center items-center">
                        <canvas id="complexityChart"></canvas>
                    </div>
                </div>
            </div>

            <!-- Advanced Analysis Grid -->
            <div class="grid grid-cols-1 gap-8 mb-8">
                
                <!-- Environment Variables -->
                {{#if envVars}}
                <div class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700">
                    <div class="flex items-center mb-6">
                        <span class="text-3xl mr-3">🔐</span>
                        <h2 class="text-xl font-bold text-gray-800 dark:text-gray-100">{{t "html.envVars"}}</h2>
                    </div>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-2">
                        {{#each envVars}}
                        <div class="bg-gray-50 dark:bg-gray-900 border border-gray-200 dark:border-gray-700 px-3 py-2 rounded text-sm font-mono text-gray-700 dark:text-gray-300 flex items-center">
                            <span class="w-2 h-2 rounded-full bg-indigo-500 mr-2"></span>
                            {{this}}
                        </div>
                        {{/each}}
                    </div>
                </div>
                {{/if}}

                <!-- API Routes -->
                {{#if apiRoutes}}
                <div class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 max-h-[400px] overflow-y-auto">
                    <div class="flex items-center mb-6 sticky top-0 bg-white dark:bg-gray-800 z-10">
                        <span class="text-3xl mr-3">🌐</span>
                        <h2 class="text-xl font-bold text-gray-800 dark:text-gray-100">{{t "html.apiRoutes"}}</h2>
                    </div>
                    <div class="space-y-2">
                        {{#each apiRoutes}}
                        <div class="flex items-center p-2 rounded hover:bg-gray-50 dark:hover:bg-gray-750 transition-colors">
                            <span class="px-2 py-1 text-xs font-bold rounded mr-3 w-16 text-center
                                {{#if (eq this.method 'GET')}}bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-300
                                {{else if (eq this.method 'POST')}}bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-300
                                {{else if (eq this.method 'PUT')}}bg-yellow-100 text-yellow-800 dark:bg-yellow-900 dark:text-yellow-300
                                {{else if (eq this.method 'DELETE')}}bg-red-100 text-red-800 dark:bg-red-900 dark:text-red-300
                                {{else}}bg-gray-100 text-gray-800 dark:bg-gray-700 dark:text-gray-300{{/if}}">
                                {{this.method}}
                            </span>
                            <code class="text-sm font-mono text-gray-700 dark:text-gray-300 flex-1">{{this.path}}</code>
                            <span class="text-xs text-gray-400 hidden md:block">{{this.file}}:{{this.line}}</span>
                        </div>
                        {{/each}}
                    </div>
                </div>
                {{/if}}

            </div>
            
            <!-- Database & Infrastructure -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-8 mb-8">
                <!-- Database -->
                {{#if database}}
                <div class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700">
                    <div class="flex items-center mb-6">
                        <span class="text-3xl mr-3">🗄️</span>
                        <h2 class="text-xl font-bold text-gray-800 dark:text-gray-100">{{t "html.database"}}</h2>
                    </div>
                    <div class="space-y-4">
                        <div class="flex justify-between items-center bg-gray-50 dark:bg-gray-900 p-3 rounded">
                            <span class="text-sm font-medium text-gray-500 dark:text-gray-400">{{t "html.detectedType"}}</span>
                            <span class="px-3 py-1 rounded-full text-sm font-bold bg-blue-100 text-blue-800 dark:bg-blue-900 dark:text-blue-300">{{database.type}}</span>
                        </div>
                        {{#if database.schemaFile}}
                        <div class="flex justify-between items-center bg-gray-50 dark:bg-gray-900 p-3 rounded">
                            <span class="text-sm font-medium text-gray-500 dark:text-gray-400">{{t "html.schemaFile"}}</span>
                            <code class="text-sm font-mono text-gray-700 dark:text-gray-300">{{database.schemaFile}}</code>
                        </div>
                        {{/if}}
                        {{#if database.models}}
                        <div>
                            <span class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-2 block">{{t "html.modelsTables"}}</span>
                            <div class="flex flex-wrap gap-2">
                                {{#each database.models}}
                                <span class="px-2 py-1 rounded text-xs font-mono bg-indigo-50 text-indigo-700 dark:bg-indigo-900/30 dark:text-indigo-300 border border-indigo-100 dark:border-indigo-800">{{this}}</span>
                                {{/each}}
                            </div>
                        </div>
                        {{/if}}
                    </div>
                </div>
                {{/if}}

                <!-- Infrastructure -->
                {{#if infrastructure}}
                <div class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700">
                    <div class="flex items-center mb-6">
                        <span class="text-3xl mr-3">🚀</span>
                        <h2 class="text-xl font-bold text-gray-800 dark:text-gray-100">{{t "html.infrastructure"}}</h2>
                    </div>
                    <div class="space-y-4">
                        <!-- Docker -->
                         <div class="mb-4">
                            <h3 class="text-sm font-bold text-gray-700 dark:text-gray-300 mb-2 flex items-center">
                                <span class="w-2 h-2 rounded-full {{#if infrastructure.docker.hasDockerfile}}bg-green-500{{else}}bg-gray-300{{/if}} mr-2"></span>
                                Docker
                            </h3>
                            {{#if infrastructure.docker.hasDockerfile}}
                            <div class="bg-gray-50 dark:bg-gray-900 p-3 rounded text-sm space-y-2">
                                {{#if infrastructure.docker.baseImage}}
                                <div class="flex justify-between">
                                    <span class="text-gray-500 dark:text-gray-400">Base Image:</span>
                                    <code class="font-mono text-gray-800 dark:text-gray-200">{{infrastructure.docker.baseImage}}</code>
                                </div>
                                {{/if}}
                                {{#if infrastructure.docker.hasCompose}}
                                <div class="flex justify-between">
                                    <span class="text-gray-500 dark:text-gray-400">Docker Compose:</span>
                                    <span class="text-green-600 dark:text-green-400 font-bold">{{t "html.detected"}}</span>
                                </div>
                                {{#if infrastructure.docker.services}}
                                <div class="mt-2">
                                    <span class="text-xs text-gray-400 block mb-1">Servizi:</span>
                                    <div class="flex flex-wrap gap-1">
                                        {{#each infrastructure.docker.services}}
                                        <span class="px-2 py-0.5 bg-blue-50 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 rounded text-xs border border-blue-100 dark:border-blue-800">{{this}}</span>
                                        {{/each}}
                                    </div>
                                </div>
                                {{/if}}
                                {{/if}}
                            </div>
                            {{else}}
                            <span class="text-sm text-gray-400 italic">{{t "html.noDocker"}}</span>
                            {{/if}}
                        </div>

                        <!-- CI/CD -->
                        <div>
                            <h3 class="text-sm font-bold text-gray-700 dark:text-gray-300 mb-2 flex items-center">
                                <span class="w-2 h-2 rounded-full {{#if (eq infrastructure.ci.provider 'None')}}bg-gray-300{{else}}bg-green-500{{/if}} mr-2"></span>
                                CI/CD Pipeline
                            </h3>
                            {{#if (eq infrastructure.ci.provider 'None')}}
                            <span class="text-sm text-gray-400 italic">{{t "html.noPipeline"}}</span>
                            {{else}}
                            <div class="bg-gray-50 dark:bg-gray-900 p-3 rounded text-sm space-y-2">
                                <div class="flex justify-between">
                                    <span class="text-gray-500 dark:text-gray-400">Provider:</span>
                                    <span class="font-bold text-gray-800 dark:text-gray-200">{{infrastructure.ci.provider}}</span>
                                </div>
                                {{#if infrastructure.ci.workflows}}
                                <div class="mt-2">
                                    <span class="text-xs text-gray-400 block mb-1">Workflows:</span>
                                    <ul class="list-disc list-inside text-xs text-gray-600 dark:text-gray-300">
                                        {{#each infrastructure.ci.workflows}}
                                        <li>{{this}}</li>
                                        {{/each}}
                                    </ul>
                                </div>
                                {{/if}}
                            </div>
                            {{/if}}
                        </div>
                    </div>
                </div>
                {{/if}}
            </div>
            
            <!-- TODOs & FIXMEs -->
            {{#if todos}}
            <div class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 mb-8">
                 <div class="flex items-center mb-6">
                    <span class="text-3xl mr-3">📝</span>
                    <h2 class="text-xl font-bold text-gray-800 dark:text-gray-100">TODOs & FIXMEs</h2>
                </div>
                <div class="grid grid-cols-1 gap-4">
                    {{#each todos}}
                    <div class="p-4 rounded-lg border {{#if (eq this.type 'FIXME')}}bg-red-50 border-red-100 dark:bg-red-900/10 dark:border-red-800{{else}}bg-yellow-50 border-yellow-100 dark:bg-yellow-900/10 dark:border-yellow-800{{/if}}">
                        <div class="flex justify-between items-start mb-2">
                             <span class="text-xs font-bold px-2 py-0.5 rounded {{#if (eq this.type 'FIXME')}}bg-red-100 text-red-800 dark:bg-red-900 dark:text-red-200{{else}}bg-yellow-100 text-yellow-800 dark:bg-yellow-900 dark:text-yellow-200{{/if}}">
                                {{this.type}}
                            </span>
                            <span class="text-xs text-gray-400 font-mono">{{this.file}}:{{this.line}}</span>
                        </div>
                        <p class="text-sm text-gray-700 dark:text-gray-300">{{this.text}}</p>
                    </div>
                    {{/each}}
                </div>
            </div>
            {{/if}}
            
             <!-- Architecture Section -->
            <div id="architecture" class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 mb-8 scroll-mt-20">
                <div class="flex items-center mb-6">
                    <span class="text-3xl mr-3">🏗️</span>
                    <h2 class="text-2xl font-bold text-gray-800 dark:text-gray-100">{{t "html.projectArchitecture"}}</h2>
                </div>
                
                <div class="grid grid-cols-1 gap-8">
                    {{#if architecture.entryPoints}}
                    <div class="space-y-4">
                        <h3 class="text-lg font-semibold text-gray-700 dark:text-gray-300">Entry Points</h3>
                        <div class="bg-gray-50 dark:bg-gray-900 rounded-lg p-4 border border-gray-200 dark:border-gray-700">
                            <ul class="space-y-2">
                                {{#each architecture.entryPoints}}
                                <li class="flex items-center text-gray-600 dark:text-gray-300 font-mono text-sm">
                                    <span class="w-2 h-2 rounded-full bg-red-500 mr-3"></span>
                                    {{this}}
                                </li>
                                {{/each}}
                            </ul>
                        </div>
                    </div>
                    {{/if}}

                    {{#if architecture.patterns}}
                    <div class="space-y-4">
                        <h3 class="text-lg font-semibold text-gray-700 dark:text-gray-300">Pattern</h3>
                        <div class="flex flex-wrap gap-2">
                            {{#each architecture.patterns}}
                            <span class="px-3 py-1 rounded-full text-sm font-medium bg-green-100 text-green-800 dark:bg-green-900 dark:text-green-300 border border-green-200 dark:border-green-800">
                                {{this}}
                            </span>
                            {{/each}}
                        </div>
                    </div>
                    {{/if}}
                </div>
                
                {{#if architecture.layers}}
                <div class="mt-8">
                    <h3 class="text-lg font-semibold text-gray-700 dark:text-gray-300 mb-4">{{t "html.layerStructure"}}</h3>
                    <div class="grid gap-4">
                    {{#each architecture.layers}}
                        <div class="relative pl-6 border-l-2 border-indigo-500 dark:border-indigo-400">
                            <h4 class="font-bold text-indigo-700 dark:text-indigo-400">{{this.name}}</h4>
                            <p class="text-sm text-gray-500 dark:text-gray-400 mt-1 mb-2">{{this.description}}</p>
                            <div class="flex flex-wrap gap-2">
                                {{#each this.files}}
                                <span class="text-xs bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-300 px-2 py-1 rounded font-mono">{{this}}</span>
                                {{/each}}
                            </div>
                        </div>
                    {{/each}}
                    </div>
                </div>
                {{/if}}
            </div>

            <!-- Mind Map Section -->
            <div id="mindmap" class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 mb-8 scroll-mt-20">
                <div class="flex justify-between items-center mb-4">
                     <div class="flex items-center">
                        <span class="text-3xl mr-3">🗺️</span>
                        <h2 class="text-2xl font-bold text-gray-800 dark:text-gray-100">{{t "html.fileMap"}}</h2>
                    </div>
                    <span class="text-xs text-gray-500 dark:text-gray-400 bg-gray-100 dark:bg-gray-700 px-3 py-1 rounded-full">{{t "html.interactive"}}</span>
                </div>
                <div class="w-full h-[600px] overflow-hidden border border-gray-200 dark:border-gray-700 rounded-xl bg-slate-50 dark:bg-gray-900 shadow-inner" id="d3-mindmap"></div>
            </div>

            <!-- Dependencies -->
            <div id="dependencies" class="bg-white dark:bg-gray-800 p-8 rounded-xl shadow-sm border border-gray-100 dark:border-gray-700 mb-8 scroll-mt-20">
                <h2 class="text-2xl font-bold text-gray-800 dark:text-gray-100 mb-6 flex items-center">
                    <span class="mr-3">📦</span> {{t "html.dependencies"}}
                </h2>
                <div class="grid grid-cols-1 gap-8">
                     {{#if dependencies.production}}
                    <div>
                        <h3 class="text-sm font-bold uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-4">{{t "readme.production"}}</h3>
                        <ul class="divide-y divide-gray-100 dark:divide-gray-700">
                            {{#each dependencies.production}}
                            <li class="py-2 flex justify-between items-center group hover:bg-gray-50 dark:hover:bg-gray-750 px-2 rounded transition-colors">
                                <span class="font-medium text-gray-700 dark:text-gray-200 group-hover:text-blue-600 dark:group-hover:text-blue-400">{{@key}}</span>
                                <span class="text-sm font-mono text-gray-500 dark:text-gray-400 bg-gray-100 dark:bg-gray-700 px-2 py-0.5 rounded">{{this}}</span>
                            </li>
                            {{/each}}
                        </ul>
                    </div>
                    {{/if}}
                    {{#if dependencies.development}}
                    <div>
                        <h3 class="text-sm font-bold uppercase tracking-wider text-gray-500 dark:text-gray-400 mb-4">{{t "readme.development"}}</h3>
                        <ul class="divide-y divide-gray-100 dark:divide-gray-700">
                             {{#each dependencies.development}}
                            <li class="py-2 flex justify-between items-center group hover:bg-gray-50 dark:hover:bg-gray-750 px-2 rounded transition-colors">
                                <span class="font-medium text-gray-700 dark:text-gray-200 group-hover:text-amber-600 dark:group-hover:text-amber-400">{{@key}}</span>
                                <span class="text-sm font-mono text-gray-500 dark:text-gray-400 bg-gray-100 dark:bg-gray-700 px-2 py-0.5 rounded">{{this}}</span>
                            </li>
                            {{/each}}
                        </ul>
                    </div>
                     {{/if}}
                </div>
            </div>

            <!-- Detailed Analysis with Search -->
            <div id="files" class="scroll-mt-20">
                <div class="flex flex-col md:flex-row justify-between items-center mb-8">
                     <h2 class="text-2xl font-bold text-gray-800 dark:text-gray-100 flex items-center mb-4 md:mb-0">
                        <span class="mr-3">📄</span> {{t "html.detailedAnalysis"}}
                    </h2>
                    
                    <!-- Search Bar -->
                    <div class="relative w-full md:w-96">
                        <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                            <svg class="w-4 h-4 text-gray-500 dark:text-gray-400" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 20 20">
                                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m19 19-4-4m0-7A7 7 0 1 1 1 8a7 7 0 0 1 14 0Z"/>
                            </svg>
                        </div>
                        <input type="text" id="file-search" class="block w-full p-2.5 pl-10 text-sm text-gray-900 border border-gray-300 rounded-lg bg-gray-50 focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-800 dark:border-gray-700 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" placeholder="{{t "html.searchPlaceholder"}}" required>
                    </div>
                </div>

                <div id="file-grid" class="grid grid-cols-1 gap-6">
                    {{#each fileAnalyses}}
                    <div class="file-card group bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700 overflow-hidden hover:shadow-md transition-all duration-300" data-filename="{{this.filePath}}" data-summary="{{this.summary}}">
                        <!-- Card Header -->
                        <div class="px-6 py-4 bg-gray-50 dark:bg-gray-750 border-b border-gray-100 dark:border-gray-700 flex justify-between items-center bg-gray-50/50 dark:bg-gray-800/50">
                            <div class="flex items-center overflow-hidden">
                                <span class="bg-blue-100 text-blue-800 text-xs font-medium mr-2 px-2.5 py-0.5 rounded dark:bg-blue-900 dark:text-blue-300 font-mono">{{this.language}}</span>
                                <h3 class="text-lg font-bold text-gray-800 dark:text-white font-mono truncate" title="{{this.filePath}}">{{this.filePath}}</h3>
                            </div>
                             <div class="flex items-center space-x-2">
                                <span class="text-xs text-gray-500 dark:text-gray-400 font-mono">{{this.lines}} LOC</span>
                                <div class="w-2 h-2 rounded-full {{#if (eq this.complexity 'high')}}bg-red-500{{else if (eq this.complexity 'medium')}}bg-yellow-500{{else}}bg-green-500{{/if}}" title="{{t "html.complexityTitle"}}: {{this.complexity}}"></div>
                                {{#if this.qualityScore}}
                                <span class="bg-indigo-100 text-indigo-800 text-xs font-medium px-2 py-0.5 rounded dark:bg-indigo-900 dark:text-indigo-300" title="{{t "html.qualityScoreTitle"}}: {{this.qualityScore}}/10">QS: {{this.qualityScore}}</span>
                                {{/if}}
                             </div>
                        </div>

                        <div class="p-6">
                            <!-- Purpose -->
                            <div class="mb-5">
                                <h4 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-2">{{t "html.purpose"}}</h4>
                                <p class="text-gray-600 dark:text-gray-300 text-sm leading-relaxed">{{this.summary}}</p>
                            </div>

                            <!-- Key Functions -->
                            {{#if this.keyFunctions}}
                            <div class="mb-5">
                                <h4 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-3">{{t "html.keyFunctions"}}</h4>
                                <div class="space-y-2">
                                    {{#each this.keyFunctions}}
                                    <div class="bg-gray-50 dark:bg-gray-900/50 rounded-lg p-3 border border-gray-100 dark:border-gray-700">
                                        <div class="flex justify-between items-start mb-1">
                                            <code class="text-sm font-bold text-indigo-600 dark:text-indigo-400 font-mono">{{this.name}}</code>
                                        </div>
                                        <p class="text-xs text-gray-500 dark:text-gray-400">{{this.explanation}}</p>
                                    </div>
                                    {{/each}}
                                </div>
                            </div>
                            {{/if}}

                            <div class="grid grid-cols-1 gap-4 mt-4 pt-4 border-t border-gray-100 dark:border-gray-700">
                                {{#if this.dependencies}}
                                <div>
                                    <h4 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-2">{{t "html.dependencies"}}</h4>
                                    <p class="text-xs font-mono text-gray-500 dark:text-gray-400 break-all bg-gray-50 dark:bg-gray-900 p-2 rounded">{{this.dependencies}}</p>
                                </div>
                                {{/if}}
                                
                                {{#if this.improvements}}
                                <div>
                                    <h4 class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-2">{{t "html.improvements"}}</h4>
                                    <ul class="list-disc list-inside text-xs text-amber-600 dark:text-amber-400 space-y-1">
                                        {{#each this.improvements}}
                                        <li>{{this}}</li>
                                        {{/each}}
                                    </ul>
                                </div>
                                {{/if}}
                            </div>
                        </div>
                    </div>
                    {{/each}}
                </div>
                 <!-- No results state -->
                 <div id="no-results" class="hidden text-center py-12">
                     <p class="text-xl text-gray-400 dark:text-gray-500">{{t "html.noFilesFound"}}</p>
                 </div>
            </div>

             <footer class="flex flex-col items-center justify-center mt-12 pt-8 border-t border-gray-200 dark:border-gray-700 text-center text-gray-400 text-sm">
                <p>&copy; {{generatedYear}} {{t "html.generatedBy"}}</p>
                <p>{{t "html.createdBy"}}</p>
            </footer>

        </div>
    </div>

    <!-- Scripts -->
    <script>
        // Dark Mode Toggle Logic
        var themeToggleDarkIcon = document.getElementById('theme-toggle-dark-icon');
        var themeToggleLightIcon = document.getElementById('theme-toggle-light-icon');

        // Change the icons inside the button based on previous settings
        if (localStorage.getItem('color-theme') === 'dark' || (!('color-theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            themeToggleLightIcon.classList.remove('hidden');
            document.documentElement.classList.add('dark');
        } else {
            themeToggleDarkIcon.classList.remove('hidden');
            document.documentElement.classList.remove('dark');
        }

        var themeToggleBtn = document.getElementById('theme-toggle');

        themeToggleBtn.addEventListener('click', function() {
            // toggle icons inside button
            themeToggleDarkIcon.classList.toggle('hidden');
            themeToggleLightIcon.classList.toggle('hidden');

            // if set via local storage previously
            if (localStorage.getItem('color-theme')) {
                if (localStorage.getItem('color-theme') === 'light') {
                    document.documentElement.classList.add('dark');
                    localStorage.setItem('color-theme', 'dark');
                } else {
                    document.documentElement.classList.remove('dark');
                    localStorage.setItem('color-theme', 'light');
                }

            // if NOT set via local storage previously
            } else {
                if (document.documentElement.classList.contains('dark')) {
                    document.documentElement.classList.remove('dark');
                    localStorage.setItem('color-theme', 'light');
                } else {
                    document.documentElement.classList.add('dark');
                    localStorage.setItem('color-theme', 'dark');
                }
            }
        });

        // Search Functionality
        const searchInput = document.getElementById('file-search');
        const fileCards = document.querySelectorAll('.file-card');
        const noResults = document.getElementById('no-results');

        searchInput.addEventListener('keyup', function(e) {
            const term = e.target.value.toLowerCase();
            let hasResults = false;

            fileCards.forEach(card => {
                const filename = card.getAttribute('data-filename').toLowerCase();
                const summary = card.getAttribute('data-summary').toLowerCase();
                
                if(filename.includes(term) || summary.includes(term)) {
                    card.classList.remove('hidden');
                    hasResults = true;
                } else {
                    card.classList.add('hidden');
                }
            });

            if(!hasResults) {
                noResults.classList.remove('hidden');
            } else {
                noResults.classList.add('hidden');
            }
        });

        // Chart.js Initialization
        document.addEventListener('DOMContentLoaded', function() {
            // Chart.js global defaults
            Chart.defaults.font.family = "'Inter', sans-serif";
            Chart.defaults.color = document.documentElement.classList.contains('dark') ? '#9ca3af' : '#4b5563';
            
            // Languages Chart
            const languageCtx = document.getElementById('languagesChart').getContext('2d');
            const languagesData = {{{languages}}};
            
            new Chart(languageCtx, {
                type: 'doughnut',
                data: {
                    labels: Object.keys(languagesData),
                    datasets: [{
                        data: Object.values(languagesData),
                        backgroundColor: [
                            '#3b82f6', '#8b5cf6', '#10b981', '#f59e0b', '#ef4444', '#6366f1'
                        ],
                        borderWidth: 0
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: {
                            position: 'right',
                            labels: {
                                usePointStyle: true,
                                boxWidth: 6
                            }
                        }
                    }
                }
            });

            // Complexity Chart
            const complexityCtx = document.getElementById('complexityChart').getContext('2d');
            const complexityData = {{{complexity}}};
            
            new Chart(complexityCtx, {
                type: 'bar',
                data: {
                    labels: ['Low', 'Medium', 'High'],
                    datasets: [{
                        label: '{{t "html.chartFilesNumber"}}',
                        data: [
                            complexityData.low || 0, 
                            complexityData.medium || 0, 
                            complexityData.high || 0
                        ],
                        backgroundColor: [
                            '#10b981', '#f59e0b', '#ef4444'
                        ],
                        borderRadius: 4
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                        y: {
                            beginAtZero: true,
                            grid: {
                                display: true,
                                color: document.documentElement.classList.contains('dark') ? 'rgba(255, 255, 255, 0.1)' : 'rgba(0, 0, 0, 0.05)'
                            }
                        },
                        x: {
                            grid: {
                                display: false
                            }
                        }
                    },
                    plugins: {
                        legend: {
                            display: false
                        }
                    }
                }
            });

            // Quality Score Chart
            const qualityCtx = document.getElementById('qualityChart').getContext('2d');
            const qualityData = {{{qualityScores}}};
            
            // Fill missing scores 1-10 with 0
            const scores = Array.from({length: 10}, (_, i) => i + 1);
            const scoreCounts = scores.map(s => qualityData[s] || 0);

            new Chart(qualityCtx, {
                type: 'bar',
                data: {
                    labels: scores,
                    datasets: [{
                        label: '{{t "html.chartFilesScore"}}',
                        data: scoreCounts,
                        backgroundColor: scoreCounts.map((_, i) => {
                            // Gradient from Red (low score) to Green (high score)
                            const hue = (i / 9) * 120; // 0 to 120
                            return `hsl(${hue}, 70%, 50%)`;
                        }),
                        borderRadius: 4
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    scales: {
                        y: {
                            beginAtZero: true,
                            grid: {
                                display: true,
                                color: document.documentElement.classList.contains('dark') ? 'rgba(255, 255, 255, 0.1)' : 'rgba(0, 0, 0, 0.05)'
                            }
                        },
                        x: {
                            grid: {
                                display: false
                            },
                            title: {
                                display: true,
                                text: '{{t "html.chartScoreLabel"}}'
                            }
                        }
                    },
                    plugins: {
                        legend: {
                            display: false
                        }
                    }
                }
            });
        });
    </script>
    
    <!-- D3 Script -->
    <script src="https://d3js.org/d3.v7.min.js"></script>
    <script>
        const data = {{{fileTree}}};
        let i = 0;
        
        const width = document.getElementById("d3-mindmap").parentElement.clientWidth;
        const height = 600;
        
        const svg = d3.select("#d3-mindmap")
            .append("svg")
            .attr("width", "100%")
            .attr("height", "100%")
            .attr("viewBox", [-50, -height / 2, width, height])
            .call(d3.zoom().on("zoom", (event) => {
                g.attr("transform", event.transform);
            }))
            .append("g")
            .attr("transform", `translate(50,0)`);
            
        const g = svg;
        
        // Dynamic colors based on theme
        const isDark = document.documentElement.classList.contains('dark');
        const nodeColor = '#6366f1'; 
        const nodeStroke = '#4f46e5';

        const tree = d3.tree().size([height - 60, width - 200]);
        const root = d3.hierarchy(data);
        root.x0 = height / 2;
        root.y0 = 0;

        root.children.forEach(collapse);
        update(root);

        function collapse(d) {
            if (d.children) {
                d._children = d.children;
                d._children.forEach(collapse);
                d.children = null;
            }
        }

        function update(source) {
            tree(root);
            const duration = 250;
            const nodes = root.descendants().reverse();
            const links = root.links();

            nodes.forEach(d => { d.y = d.depth * 200; });

            const node = g.selectAll('g.node')
                .data(nodes, d => d.id || (d.id = ++i));

            const nodeEnter = node.enter().append('g')
                .attr('class', 'node')
                .attr('transform', d => `translate(${source.y0},${source.x0})`)
                .on('click', click);

            nodeEnter.append('circle')
                .attr('class', 'node')
                .attr('r', 1e-6)
                .style('fill', d => d._children ? nodeColor : (document.documentElement.classList.contains('dark') ? '#1f2937' : '#fff'))
                .style('stroke', nodeStroke)
                .style('cursor', 'pointer');

            nodeEnter.append('text')
                .attr('dy', '.35em')
                .attr('x', d => d.children || d._children ? -13 : 13)
                .attr('text-anchor', d => d.children || d._children ? 'end' : 'start')
                .text(d => d.data.name)
                .style('font-family', 'Inter, sans-serif')
                .style('font-size', '12px')
                .style('fill', document.documentElement.classList.contains('dark') ? '#e5e7eb' : '#374151') 
                .style('fill-opacity', 1e-6);

            const nodeUpdate = node.merge(nodeEnter).transition().duration(duration)
                .attr('transform', d => `translate(${d.y},${d.x})`);

            nodeUpdate.select('circle').attr('r', 6).style('fill', d => d._children ? nodeColor : (document.documentElement.classList.contains('dark') ? '#1f2937' : '#fff'));
            nodeUpdate.select('text').style('fill-opacity', 1).style('fill', () => document.documentElement.classList.contains('dark') ? '#e5e7eb' : '#374151');

            const nodeExit = node.exit().transition().duration(duration)
                .attr('transform', d => `translate(${source.y},${source.x})`)
                .remove();
            nodeExit.select('circle').attr('r', 1e-6);
            nodeExit.select('text').style('fill-opacity', 1e-6);

            const link = g.selectAll('path.link').data(links, d => d.target.id);
            const linkEnter = link.enter().insert('path', 'g')
                .attr('class', 'link')
                .attr('d', d => {
                    const o = {x: source.x0, y: source.y0};
                    return diagonal(o, o);
                })
                .style('fill', 'none')
                .style('stroke', '#94a3b8')
                .style('stroke-opacity', 0.4)
                .style('stroke-width', '1.5px');

            const linkUpdate = link.merge(linkEnter).transition().duration(duration)
                .attr('d', d => diagonal(d.source, d.target));

            const linkExit = link.exit().transition().duration(duration)
                .attr('d', d => {
                    const o = {x: source.x, y: source.y};
                    return diagonal(o, o);
                })
                .remove();

            nodes.forEach(d => { d.x0 = d.x; d.y0 = d.y; });
            function diagonal(s, d) {
                return `M ${s.y} ${s.x} C ${(s.y + d.y) / 2} ${s.x}, ${(s.y + d.y) / 2} ${d.x}, ${d.y} ${d.x}`;
            }
            function click(event, d) {
                if (d.children) { d._children = d.children; d.children = null; } 
                else { d.children = d._children; d._children = null; }
                update(d);
            }
        }
    </script>
</body>
</html>
